# Secrets — ne jamais versionner de vraies valeurs
# Copier en .env et remplir localement

# Exemple API (optionnel)
# API_KEY=
# DATABASE_URL=

# Port dev override (optionnel)
# DEV_LAUNCHER_DASHBOARD_PORT=9221
# DEV_LAUNCHER_DEV_PORT=5173
