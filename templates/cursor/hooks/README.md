# Cursor hooks — template

| Event | Script |
|-------|--------|
| `beforeSubmitPrompt` | `A.B.C.X.Y - X update - prompt indent.mjs` |
| `stop` | `A.B.C.X.Y - Y update - subprompt indent.mjs` |

Opt-out : `même X` / `même Y`.

Output JSON : `executionLogLabel`, `hookName`, `versionHook.record.projectRoot`.

Après modif : **redémarrer Cursor**, workspace **trusted**.

Doc : `C:\Dev\Project\REFERENCE\docs\processes\versionnement-global.md`
